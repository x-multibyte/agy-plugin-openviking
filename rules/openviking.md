# OpenViking Memory Rules

- Query OpenViking context before asking the user for existing preferences or historical context.
- When indexing large documentation collections, use serial processing to prevent CPU saturation.
